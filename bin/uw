#!/bin/bash
#file:///C:/Users/qzrp0/AppData/Local/lxss
in=`readlink -f $1`
if [[ $in =~ ^/mnt/./* ]]; then
	drive_letter=${in:5:1}
	out=${drive_letter^}:${in:6}
	echo $out
else
	echo "C:/Users/qzrp0/AppData/Local/lxss$in"
fi

#echo $in|xsel -i
#firefox.exe "$in" 2>/dev/null
