#インデントレベルを表示しない
def sigma(list):
├ n = 0
├ for i in range(list):
│ └ for j in range(list):
│   └ if i > j:
│     └ if list[i] < list[j]:
│       └ n += 1
└ return n
#よさ気な表示方法その1
def sigma(list):
├ n = 0
├ for i in range(list):
│ ⎿₂for j in range(list):
│   ⎿₃if i > j:
│     ⎿₄if list[i] < list[j]:
│       ⎿₅n += 1
⎿₁return n

def sigma(list):
├ n = 0
├ for i in range(list):
│ └ for j in range(list):
│   └ if i > j:
│     └ if list[i] < list[j]:
│       └ n += 1
└ return n
